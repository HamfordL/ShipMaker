const SenderAddress = () => {
  const title = "This is dummy text but eventually we will have a component that collects the sender's information.";

  return (
    <div>{title}</div>
  );
};

export default SenderAddress;
